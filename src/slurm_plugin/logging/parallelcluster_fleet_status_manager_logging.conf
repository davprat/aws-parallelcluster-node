[loggers]
keys=root,events

[handlers]
keys=fileHandler,eventsHandler

[formatters]
keys=defaultFormatter,eventsFormatter

[logger_root]
level=INFO
handlers=fileHandler

[formatter_defaultFormatter]
format=%(asctime)s - [%(name)s:%(funcName)s] - %(levelname)s - %(message)s

[handler_fileHandler]
class=FileHandler
level=INFO
formatter=defaultFormatter
args=("/var/log/parallelcluster/slurm_fleet_status_manager.log",)

[logger_events]
level=INFO
handlers=eventsHandler
propagate=0
qualname=slurm_plugin.fleet_status_manager.events

[formatter_eventsFormatter]
format=%(message)s

[handler_eventsHandler]
class=FileHandler
level=INFO
formatter=eventsFormatter
args=('/var/log/parallelcluster/slurm_fleet_status_manager.events', 'w', None, False)
