[loggers]
keys=root,metrics

[handlers]
keys=fileHandler,metricsHandler

[formatters]
keys=defaultFormatter,metricsFormatter

[logger_root]
level=INFO
handlers=fileHandler

[formatter_defaultFormatter]
format=%(asctime)s - [%(name)s:%(funcName)s] - %(levelname)s - %(message)s

[handler_fileHandler]
class=FileHandler
level=INFO
formatter=defaultFormatter
args=("/var/log/parallelcluster/slurm_resume.log",)

[logger_metrics]
level=INFO
handlers=metricsHandler
propagate=0
qualname=slurm_plugin.resume.metrics

[formatter_metricsFormatter]
format=%(message)s

[handler_metricsHandler]
class=FileHandler
level=INFO
formatter=metricsFormatter
args=('/var/log/parallelcluster/slurm_resume.metrics', 'w', None, False)
